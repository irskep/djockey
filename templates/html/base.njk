<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ doc.title }}</title>
  </head>
  <body>
    <article>{{ docs.content | safe }}</article>

    <nav>{{ docs.toc | safe }}</nav>
    </body>
</html>